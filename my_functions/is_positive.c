#include <stdio.h>
char is_positive(int nb)
{
    return (nb >= 0);
}

int main()
{
    // printf("%d\n",is_positive(10));
    // printf("%d\n",is_positive(0));
    // printf("%d\n",is_positive(-1));
}

